using TradeMatchingConsoleApp.Data;
using TradeMatchingConsoleApp.Logic;

namespace TradeMatchingConsoleApp
{
    internal class Program
    {
        static void Main(string[] args)
        {
        //    var trades = TradeData.GetSampleTrades();
        //    var results = TradeMatcher.MatchAndCalculate(trades);

        //    foreach (var pnl in results)
        //    {
        //        Console.WriteLine($"P&L ID: {pnl.Id}, Profit: {pnl.ProfitOrLoss}, Days Held: {pnl.DaysHolding}, Trades: {string.Join(",", pnl.TradeIds)}");
        //    }

            Console.WriteLine("Done!");
        }
    }
}
